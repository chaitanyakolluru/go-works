package main
import "fmt"
func main() {
	fmt.Println("Addition: ", 1 + 3) //calculated as an expression and passed to the print function.
	fmt.Println("Sub: ", 4 - 3) // calculated as an expression and passed to the print function.
	fmt.Println("Mul: ", 3 + 3) // calculated as an expression and passed to the print function.
	fmt.Println("Div: ", 20 / 3) // calculated as an expression and passed to the print function.	

	fmt.Println("div: ", 20.0/3)
}